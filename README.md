## 📊 Product Price Tracker

Проект реализует автоматический сбор, хранение и визуализацию цен товаров с использованием PostgreSQL, Docker и Streamlit.

📌 О проекте

Проект имитирует систему мониторинга цен товаров.

ETL-скрипт периодически получает товары из API, немного изменяет цены для имитации изменений рынка, сохраняет историю в PostgreSQL и отображает данные в интерактивном дашборде Streamlit.

Все сервисы работают в Docker-контейнерах.

PostgreSQL — хранит данные

ETL-скрипт — качает товары и пишет в БД

Streamlit — показывает дашборд

Все это запускается через Docker Compose.

🚀 Возможности проекта
ETL
автоматическая загрузка товаров из API
периодическое обновление цен
имитация изменения цен
автоматическое переподключение к PostgreSQL
обработка ошибок API и базы данных
ограничение истории цен (100 записей на товар)
База данных
хранение истории цен
автоматическое создание таблицы
сохранение данных через Docker volume
Дашборд
таблица последних цен
средняя цена по категориям
график изменения цены товара
автоматическое обновление данных

## 🛠️ Стек
Python   
Streamlit   
PostgreSQL   
Docker / Docker Compose   
VPS (Ubuntu)   

## ⚙️ Что реализовано
Получение данных из внешнего API   
Автоматическая загрузка товаров в PostgreSQL   
ETL-процесс с периодическим обновлением данных   
Хранение истории изменения цен   
Визуализация данных в Streamlit  
Docker-контейнеризация проекта   
Автоматический перезапуск контейнеров после reboot сервера  

##📦 Источник данных

Используется тестовое API товаров:   

DummyJSON Products API   

Загружаются:   

название товара   
категория  
цена   

Для демонстрации динамики цен при каждом обновлении применяется небольшое случайное изменение стоимости.   

🔄 Как работает проект
ETL-скрипт получает товары из API
Цена немного изменяется случайным образом
Данные сохраняются в PostgreSQL
Старые записи автоматически удаляются
Streamlit получает данные из базы
На странице отображаются таблицы и графики

## 📈 Возможности дашборда   
🛒 Latest Products   

Таблица с последними актуальными ценами товаров.   

📊 Average Price by Category   

Средняя цена товаров по категориям.   

📉 Price History   

График изменения цены выбранного товара во времени.   

## 🔄 Автообновление данных

ETL-сервис автоматически:

Получает данные из API
Сохраняет новые записи в PostgreSQL
Обновляет историю цен

Обновление выполняется через заданный интервал времени.

## 🐳 Docker

Проект полностью контейнеризирован.

Контейнеры:

products_db — PostgreSQL
products_etl — ETL-процесс
products_dashboard — Streamlit dashboard

Используется:

Docker Compose
volume для хранения данных PostgreSQL
restart policy (restart: always)
## 🌐 Деплой

Проект развернут на VPS (Ubuntu).

Дашборд доступен по адресу:

http://46.149.77.76:8502   

## 📁 Структура проекта
price-tracker/   
│   
├── app/   
│   └── app.py   
│   
├── etl/   
│   └── download_products.py   
│   
├── sql/   
│   └── init.sql   
│   
├── docker-compose.yml   
├── Dockerfile   
├── requirements.txt   
└── .env   
🚀 Чему посвящен проект

Проект демонстрирует:

работу ETL-пайплайна,
интеграцию внешнего API,
хранение исторических данных,
построение аналитического дашборда,
деплой Python-приложения в Docker на VPS.
